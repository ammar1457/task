on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: "Login to dockerhub"
        run: docker login --username ${{ secrets.ammar01274861457 }} --password ${{ secrets.dckr_pat_DvMNWJUJ7mLX9vwtSCJrJsmCHtE }}
      - name: "Building Docker Image"
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/myimage .
      - name: "pushing to docker hub"
        run: docker push ${{ secrets.DOCKER_USERNAME }}/myimage